// NoBroker route removed per user request. This file is now a safe stub.
import React from "react";

export default function NoBrokerRemoved() {
  // Keep the route file present but render nothing to avoid FS route errors.
  return null;
}
